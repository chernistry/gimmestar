# Environment Configuration

# Node environment
NODE_ENV=development

# Encryption Key (32 bytes, base64 encoded)
# Generate with: openssl rand -base64 32
# REQUIRED: Application will fail to start without this key
ENCRYPTION_KEY=
