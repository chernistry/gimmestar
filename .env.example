# Environment Configuration

# Node environment
NODE_ENV=development

# Server Port
PORT=3000

# Database connection string (REQUIRED)
# Example: postgresql://user:password@localhost:5432/gimmestar
DATABASE_URL=postgresql://localhost:5432/gimmestar

# Encryption Key (32 bytes, base64 encoded)
# Generate with: openssl rand -base64 32
# REQUIRED: Application will fail to start without this key
ENCRYPTION_KEY=

# GitHub OAuth Configuration (REQUIRED)
# Register OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:3000/auth/github/callback

# Session Secret (32 bytes, base64 encoded)
# Generate with: openssl rand -base64 32
# REQUIRED: Used for JWT signing
SESSION_SECRET=
